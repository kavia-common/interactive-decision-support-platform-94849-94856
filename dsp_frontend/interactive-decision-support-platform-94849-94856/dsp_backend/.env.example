# Backend environment example for DSP Backend (FastAPI)

# Security
SECRET_KEY=please_change_me
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS: allow the frontend origin (default dev server)
CORS_ORIGINS=http://localhost:3000

# Database (default: SQLite file inside container)
DATABASE_URL=sqlite:////app/app.db

# Optional metadata
PROJECT_NAME=DSP Backend API
PROJECT_DESCRIPTION=FastAPI backend for Decision Support Platform providing auth and agent endpoints.
PROJECT_VERSION=0.1.0
