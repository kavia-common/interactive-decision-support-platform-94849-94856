# Example environment for FastAPI backend
# Copy this file to ".env" in the same directory and adjust as needed.

# Project metadata
PROJECT_NAME=DSP Backend API
PROJECT_DESCRIPTION=FastAPI backend for Decision Support Platform providing auth and agent endpoints.
PROJECT_VERSION=0.1.0

# Auth settings
SECRET_KEY=CHANGE_ME_PLEASE_IN_PROD
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS: comma-separated origins allowed to access the API
# Ensure the frontend origin is included. For local CRA dev server:
CORS_ORIGINS=http://localhost:3000

# Database URL. Default is SQLite file in project directory
# For SQLite use 'sqlite:///./app.db' for local dev
DATABASE_URL=sqlite:///./app.db
